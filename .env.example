# Local development (SQLite — zero install)
DATABASE_URL="file:./dev.db"

# Production (PostgreSQL — uncomment and set when deploying to VPS)
# DATABASE_URL="postgresql://user:password@localhost:5432/prabodha"

JWT_SECRET="change-me-to-a-random-secret"
JWT_EXPIRES_IN="7d"
